{
  "hooks": {
    "PreToolUse": [
      {
        "type": "command",
        "command": "if echo \"$toolInput\" | grep -q 'git push'; then cd \"$(git rev-parse --show-toplevel)/homework-6\" && npm test -- --coverage --coverageThreshold='{\"global\":{\"lines\":80,\"functions\":80,\"branches\":80,\"statements\":80}}' || (echo '{\"continue\": false, \"stopReason\": \"BLOCKED: Test coverage below 80% - push rejected\"}' && exit 0); fi"
      }
    ]
  }
}